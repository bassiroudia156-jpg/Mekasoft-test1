##### FILE: OnboardingStep1.jsx #####
import Icon from '@global/Icon';

export const displayName = 'Onboarding — Étape 1 : Bienvenue';
export const screenSize = 'desktop';

export default function OnboardingStep1() {
  return (
    <div className="bg-background font-body flex items-center justify-center" style={{ minHeight: '700px' }}>
      <div className="flex flex-col items-center text-center max-w-md w-full px-8 py-16">

        {/* Logo */}
        <div className="flex items-center gap-3 mb-16">
          <div className="w-10 h-10 bg-primary rounded-md flex items-center justify-center">
            <Icon i="wrench" size={20} className="text-primary-foreground" />
          </div>
          <span className="font-headings font-bold text-xl text-foreground tracking-tight">MekaSoft</span>
        </div>

        {/* Welcome text */}
        <div className="mb-12">
          <h1 className="text-3xl font-bold font-headings text-foreground mb-4">
            {t('Bienvenue sur Mekasoft')} <span className="not-italic">👋</span>
          </h1>
          <p className="text-base text-muted-foreground leading-relaxed">
            {t('Configurons votre garage en quelques minutes.')}
          </p>
        </div>

        {/* Actions */}
        <div className="flex flex-col gap-3 w-full">
          <button className="w-full bg-primary text-primary-foreground font-medium py-3 rounded-md text-base">
            {t('Commencer')}
          </button>
          <a className="text-sm text-muted-foreground font-medium py-2">
            {t('Passer pour l\'instant')}
          </a>
        </div>

      </div>
    </div>
  );
}


##### FILE: OnboardingStep3.jsx #####
import Icon from '@global/Icon';

export const displayName = 'Onboarding — Étape 3 : Équipe';
export const screenSize = 'desktop';

const teamOptions = [
  { icon: 'user', label: 'Moi uniquement', desc: 'Je gère seul mon garage' },
  { icon: 'users', label: '2–5 personnes', desc: 'Une petite équipe' },
  { icon: 'users-round', label: '6–10 personnes', desc: 'Une équipe intermédiaire' },
  { icon: 'building-2', label: 'Plus de 10', desc: 'Un grand atelier' },
];

export default function OnboardingStep3() {
  return (
    <div className="bg-background font-body flex items-center justify-center" style={{ minHeight: '700px' }}>
      <div className="flex flex-col items-center max-w-lg w-full px-8 py-12">

        {/* Logo */}
        <div className="flex items-center gap-3 mb-12">
          <div className="w-9 h-9 bg-primary rounded-md flex items-center justify-center">
            <Icon i="wrench" size={18} className="text-primary-foreground" />
          </div>
          <span className="font-headings font-bold text-lg text-foreground tracking-tight">MekaSoft</span>
        </div>

        {/* Progress */}
        <div className="w-full mb-10">
          <div className="flex items-center justify-between mb-3">
            <span className="text-xs font-medium text-muted-foreground uppercase tracking-widest">{t('Étape 3 sur 4')}</span>
          </div>
          <div className="w-full bg-muted rounded-full h-1">
            <div className="bg-primary h-1 rounded-full" style={{ width: '75%' }}></div>
          </div>
        </div>

        {/* Heading */}
        <div className="w-full mb-8">
          <h1 className="text-2xl font-bold font-headings text-foreground mb-2">{t('Qui travaille dans votre garage ?')}</h1>
          <p className="text-sm text-muted-foreground">{t('Combien de personnes travaillent actuellement dans votre garage ?')}</p>
        </div>

        {/* Options */}
        <div className="w-full flex flex-col gap-3">
          {/* First option — selected state */}
          <button className="w-full flex items-center gap-4 p-4 rounded-md border-2 border-primary bg-secondary text-left">
            <div className="w-9 h-9 bg-primary rounded-md flex items-center justify-center flex-shrink-0">
              <Icon i="user" size={16} className="text-primary-foreground" />
            </div>
            <div>
              <div className="text-sm font-semibold text-foreground">{t('Moi uniquement')}</div>
              <div className="text-xs text-muted-foreground">{t('Je gère seul mon garage')}</div>
            </div>
            <div className="ml-auto">
              <div className="w-5 h-5 bg-primary rounded-full flex items-center justify-center">
                <Icon i="check" size={11} className="text-primary-foreground" />
              </div>
            </div>
          </button>

          {/* Other options — unselected */}
          {[
            { icon: 'users', label: t('2–5 personnes'), desc: t('Une petite équipe') },
            { icon: 'users-round', label: t('6–10 personnes'), desc: t('Une équipe intermédiaire') },
            { icon: 'building-2', label: t('Plus de 10'), desc: t('Un grand atelier') },
          ].map((opt, i) => (
            <button key={i} className="w-full flex items-center gap-4 p-4 rounded-md border border-border bg-surface text-left">
              <div className="w-9 h-9 bg-muted rounded-md flex items-center justify-center flex-shrink-0">
                <Icon i={opt.icon} size={16} className="text-muted-foreground" />
              </div>
              <div>
                <div className="text-sm font-medium text-foreground">{opt.label}</div>
                <div className="text-xs text-muted-foreground">{opt.desc}</div>
              </div>
              <div className="ml-auto">
                <div className="w-5 h-5 border-2 border-border rounded-full"></div>
              </div>
            </button>
          ))}
        </div>

        {/* CTA */}
        <div className="w-full mt-10 flex flex-col gap-3">
          <button className="w-full bg-primary text-primary-foreground font-medium py-3 rounded-md text-sm">
            {t('Continuer')}
          </button>
          <a className="text-sm text-muted-foreground font-medium py-2 text-center">
            {t('Retour')}
          </a>
        </div>

      </div>
    </div>
  );
}


##### FILE: OnboardingStep4.jsx #####
import Icon from '@global/Icon';

export const displayName = 'Onboarding — Étape 4 : Prêt';
export const screenSize = 'desktop';

export default function OnboardingStep4() {
  return (
    <div className="bg-background font-body flex items-center justify-center" style={{ minHeight: '700px' }}>
      <div className="flex flex-col items-center text-center max-w-md w-full px-8 py-16">

        {/* Logo */}
        <div className="flex items-center gap-3 mb-14">
          <div className="w-9 h-9 bg-primary rounded-md flex items-center justify-center">
            <Icon i="wrench" size={18} className="text-primary-foreground" />
          </div>
          <span className="font-headings font-bold text-lg text-foreground tracking-tight">MekaSoft</span>
        </div>

        {/* Progress — complete */}
        <div className="w-full mb-12">
          <div className="flex items-center justify-between mb-3">
            <span className="text-xs font-medium text-success uppercase tracking-widest">{t('Terminé')}</span>
          </div>
          <div className="w-full bg-muted rounded-full h-1">
            <div className="bg-success h-1 rounded-full w-full"></div>
          </div>
        </div>

        {/* Success icon */}
        <div className="w-16 h-16 bg-success/10 rounded-full flex items-center justify-center mb-6">
          <Icon i="check-circle" size={32} className="text-success" />
        </div>

        {/* Text */}
        <div className="mb-10">
          <h1 className="text-2xl font-bold font-headings text-foreground mb-3">
            {t('Votre garage est prêt')} <span>🎉</span>
          </h1>
          <p className="text-sm text-muted-foreground leading-relaxed">
            {t('Vous pouvez maintenant commencer à gérer vos clients et vos véhicules avec Mekasoft.')}
          </p>
        </div>

        {/* Summary */}
        <div className="w-full bg-surface border border-border rounded-md p-4 mb-10 text-left">
          <div className="flex items-center gap-3 py-1.5">
            <div className="w-5 h-5 bg-success/10 rounded flex items-center justify-center flex-shrink-0">
              <Icon i="check" size={11} className="text-success" />
            </div>
            <span className="text-sm text-foreground">{t('Garage Dakar Auto — configuré')}</span>
          </div>
          <div className="flex items-center gap-3 py-1.5">
            <div className="w-5 h-5 bg-success/10 rounded flex items-center justify-center flex-shrink-0">
              <Icon i="check" size={11} className="text-success" />
            </div>
            <span className="text-sm text-foreground">{t('Profil utilisateur — activé')}</span>
          </div>
          <div className="flex items-center gap-3 py-1.5">
            <div className="w-5 h-5 bg-success/10 rounded flex items-center justify-center flex-shrink-0">
              <Icon i="check" size={11} className="text-success" />
            </div>
            <span className="text-sm text-foreground">{t('Accès MekaSoft — prêt')}</span>
          </div>
        </div>

        {/* Actions */}
        <div className="flex flex-col gap-3 w-full">
          <button className="w-full bg-accent text-accent-foreground font-semibold py-3 rounded-md text-base flex items-center justify-center gap-2">
            <Icon i="user-plus" size={16} />
            {t('Ajouter mon premier client')}
          </button>
          <a className="text-sm text-primary font-medium py-2">
            {t('Découvrir le tableau de bord')}
          </a>
        </div>

      </div>
    </div>
  );
}


##### FILE: OnboardingWelcome.jsx #####
import Icon from '@global/Icon';

export const displayName = 'Onboarding — Vue d\'ensemble (4 étapes)';
export const screenSize = 'desktop';

export default function OnboardingOverview() {
  return (
    <div className="bg-background font-body flex" style={{ minHeight: '700px' }}>

      {/* Left panel — branding */}
      <div className="w-80 bg-primary flex flex-col justify-between p-10 flex-shrink-0">
        <div>
          <div className="flex items-center gap-3 mb-16">
            <div className="w-9 h-9 bg-accent rounded-md flex items-center justify-center">
              <Icon i="wrench" size={18} className="text-accent-foreground" />
            </div>
            <span className="font-headings font-bold text-lg text-primary-foreground tracking-tight">MekaSoft</span>
          </div>
          <div className="text-primary-foreground/50 text-xs uppercase tracking-widest mb-3">{t('Démarrage')}</div>
          <h2 className="text-2xl font-bold font-headings text-primary-foreground mb-6 leading-snug">{t('Votre atelier, organisé dès le premier jour.')}</h2>
          <p className="text-sm text-primary-foreground/60 leading-relaxed">{t('MekaSoft est conçu pour les garages qui veulent aller à l\'essentiel. Suivez les 4 étapes rapides pour démarrer.')}</p>
        </div>

        {/* Steps indicator */}
        <div className="flex flex-col gap-4">
          {[
            { num: '01', label: t('Bienvenue') },
            { num: '02', label: t('Votre garage') },
            { num: '03', label: t('Votre équipe') },
            { num: '04', label: t('C\'est parti') },
          ].map((step, i) => (
            <div key={i} className="flex items-center gap-3">
              <div className={`w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold flex-shrink-0 ${i === 0 ? 'bg-accent text-accent-foreground' : 'bg-primary-foreground/10 text-primary-foreground/40'}`}>
                {step.num}
              </div>
              <span className={`text-sm font-medium ${i === 0 ? 'text-primary-foreground' : 'text-primary-foreground/40'}`}>{step.label}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Right panel — step 1 content */}
      <div className="flex-1 flex items-center justify-center p-16">
        <div className="flex flex-col items-center text-center max-w-sm w-full">

          {/* Emoji */}
          <div className="text-5xl mb-8">👋</div>

          {/* Text */}
          <h1 className="text-3xl font-bold font-headings text-foreground mb-4">
            {t('Bienvenue sur Mekasoft')}
          </h1>
          <p className="text-base text-muted-foreground leading-relaxed mb-12">
            {t('Configurons votre garage en quelques minutes.')}
          </p>

          {/* Actions */}
          <div className="flex flex-col gap-3 w-full">
            <button className="w-full bg-primary text-primary-foreground font-semibold py-3 rounded-md text-base">
              {t('Commencer')}
            </button>
            <a className="text-sm text-muted-foreground font-medium py-2">
              {t('Passer pour l\'instant')}
            </a>
          </div>

        </div>
      </div>
    </div>
  );
}


##### FILE: OnboardingWelcome_next1.jsx #####
import Icon from '@global/Icon';
import FormField from '@components/FormField';

export const displayName = 'Onboarding — Étape 2 : Infos garage';
export const screenSize = 'desktop';

export default function OnboardingStep2() {
  return (
    <div className="bg-background font-body flex" style={{ minHeight: '700px' }}>

      {/* Left panel — branding */}
      <div className="w-80 bg-primary flex flex-col justify-between p-10 flex-shrink-0">
        <div>
          <div className="flex items-center gap-3 mb-16">
            <div className="w-9 h-9 bg-accent rounded-md flex items-center justify-center">
              <Icon i="wrench" size={18} className="text-accent-foreground" />
            </div>
            <span className="font-headings font-bold text-lg text-primary-foreground tracking-tight">MekaSoft</span>
          </div>
          <div className="text-primary-foreground/50 text-xs uppercase tracking-widest mb-3">{t('Démarrage')}</div>
          <h2 className="text-2xl font-bold font-headings text-primary-foreground mb-6 leading-snug">{t('Votre atelier, organisé dès le premier jour.')}</h2>
          <p className="text-sm text-primary-foreground/60 leading-relaxed">{t('MekaSoft est conçu pour les garages qui veulent aller à l\'essentiel. Suivez les 4 étapes rapides pour démarrer.')}</p>
        </div>

        {/* Steps indicator */}
        <div className="flex flex-col gap-4">
          {[
            { num: '01', label: t('Bienvenue') },
            { num: '02', label: t('Votre garage') },
            { num: '03', label: t('Votre équipe') },
            { num: '04', label: t('C\'est parti') },
          ].map((step, i) => (
            <div key={i} className="flex items-center gap-3">
              <div className={`w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold flex-shrink-0 ${i === 1 ? 'bg-accent text-accent-foreground' : i < 1 ? 'bg-success text-success-foreground' : 'bg-primary-foreground/10 text-primary-foreground/40'}`}>
                {i < 1 ? <Icon i="check" size={12} /> : step.num}
              </div>
              <span className={`text-sm font-medium ${i <= 1 ? 'text-primary-foreground' : 'text-primary-foreground/40'}`}>{step.label}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Right panel — step 2 content */}
      <div className="flex-1 flex items-center justify-center p-16">
        <div className="flex flex-col max-w-sm w-full">

          {/* Progress bar */}
          <div className="mb-12">
            <div className="flex items-center justify-between mb-3">
              <span className="text-xs font-medium text-muted-foreground uppercase tracking-widest">{t('Étape 2 sur 4')}</span>
              <span className="text-xs font-medium text-muted-foreground">50%</span>
            </div>
            <div className="w-full bg-muted rounded-full h-1.5">
              <div className="bg-primary h-1.5 rounded-full" style={{ width: '50%' }}></div>
            </div>
          </div>

          {/* Heading */}
          <h1 className="text-2xl font-bold font-headings text-foreground mb-3">
            {t('Parlons de votre garage.')}
          </h1>
          <p className="text-sm text-muted-foreground leading-relaxed mb-8">
            {t('Ces informations permettent d\'identifier votre garage. Vous pourrez les modifier plus tard.')}
          </p>

          {/* Form fields */}
          <div className="flex flex-col gap-5 mb-10">
            <FormField
              label={t('Nom du garage')}
              placeholder={t('Garage Dakar Auto')}
              required={true}
            />
            <FormField
              label={t('Téléphone')}
              placeholder={t('77 123 45 67')}
              required={true}
            />
            <FormField
              label={t('Ville')}
              placeholder={t('Dakar')}
              required={true}
            />
          </div>

          {/* Actions */}
          <div className="flex flex-col gap-3">
            <button className="w-full bg-primary text-primary-foreground font-semibold py-3 rounded-md text-base">
              {t('Continuer')}
            </button>
            <a className="text-sm text-muted-foreground font-medium py-2 text-center">
              {t('Retour')}
            </a>
          </div>

        </div>
      </div>
    </div>
  );
}


##### FILE: DashboardFirstLaunch.jsx #####
import Icon from '@global/Icon';
import Sidebar from '@components/Sidebar';
import TopBar from '@components/TopBar';
import StatCard from '@components/StatCard';
import RevenueChart from '@components/RevenueChart';
import QuickActions from '@components/QuickActions';
import InterventionRow from '@components/InterventionRow';
import InterventionFilters from '@components/InterventionFilters';

export const displayName = 'Dashboard — Premier lancement (onboarding ignoré)';
export const screenSize = 'desktop';

const interventions = [];

export default function DashboardFirstLaunch() {
  return (
    <div className="flex bg-background font-body min-h-full">
      <Sidebar active="dashboard" />
      <div className="flex flex-col flex-1">
        <TopBar />

        {/* Onboarding skipped banner */}
        <div className="mx-6 mt-5 bg-secondary border border-secondary-foreground/20 rounded-md px-5 py-3 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <Icon i="info" size={16} className="text-secondary-foreground flex-shrink-0" />
            <span className="text-sm text-secondary-foreground">{t('Vous pouvez configurer votre garage à tout moment depuis les')}</span>
            <a className="text-sm font-semibold text-primary">{t('Paramètres')}</a>
          </div>
          <button className="w-6 h-6 flex items-center justify-center rounded text-muted-foreground">
            <Icon i="x" size={14} />
          </button>
        </div>

        <div className="flex flex-col gap-5 p-6">
          {/* Empty state hero */}
          <div className="bg-surface border border-border rounded-md p-12 flex flex-col items-center text-center">
            <div className="w-14 h-14 bg-secondary rounded-full flex items-center justify-center mb-5">
              <Icon i="wrench" size={24} className="text-primary" />
            </div>
            <h2 className="text-xl font-bold font-headings text-foreground mb-2">{t('Bienvenue sur MekaSoft')}</h2>
            <p className="text-sm text-muted-foreground max-w-xs leading-relaxed mb-8">{t('Votre tableau de bord est vide pour l\'instant. Commencez par ajouter un client ou une intervention.')}</p>
            <div className="flex gap-3">
              <button className="bg-accent text-accent-foreground font-semibold px-5 py-2.5 rounded-md text-sm flex items-center gap-2">
                <Icon i="user-plus" size={14} />
                {t('Ajouter un client')}
              </button>
              <button className="bg-primary text-primary-foreground font-medium px-5 py-2.5 rounded-md text-sm flex items-center gap-2">
                <Icon i="plus" size={14} />
                {t('Nouvelle intervention')}
              </button>
            </div>
          </div>

          {/* KPIs — all zeros */}
          <div className="flex gap-4">
            <StatCard label={t('En cours')} value="0" sub={t('interventions actives')} status="warning" icon="wrench" />
            <StatCard label={t('Non payé')} value="0" sub={t('à encaisser')} status="accent" icon="banknote" />
            <StatCard label={t('Terminé ce mois')} value="0" sub={t('interventions clôturées')} status="success" icon="check-circle" />
            <StatCard label={t('Recettes du mois')} value="—" sub={t('FCFA encaissés')} status="default" icon="trending-up" />
          </div>

          {/* Quick actions */}
          <QuickActions />
        </div>
      </div>
    </div>
  );
}

